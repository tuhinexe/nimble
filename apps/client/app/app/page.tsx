import { ThemeSwitch } from "@nimble/components/theme-switch";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      App
      <ThemeSwitch />
    </div>
  );
};

export default page;
